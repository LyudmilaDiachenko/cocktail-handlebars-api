{{#each this.drinks}}
    <li class="card-list">
        <div class="img-container">
            <img src="{{strDrinkThumb}}" alt="фото" class="card-img">
            <p class="text-title">{{strDrink}}</p>
        </div>
    </li>
{{/each}}